## What this changes

<!-- One or two sentences. If this corrects a state record, name the state and the field. -->

## Sources

<!--
Cite a primary source for every factual claim: link, publisher and date. Say what
the source establishes, not only that it exists. A rating change needs evidence
that contradicts the current rating, and the reasoning belongs here rather than in
the number alone.
-->

## Checklist

- [ ] `python3 tests/validate_data.py` passes
- [ ] `node --test tests/model.test.cjs` passes
- [ ] `python3 build.py` was run and the regenerated `index.html` is committed
- [ ] Rating changes explain the reasoning above, and cite a primary source
